/**
 * Deploy-Zeit-Konfiguration. Diese Werte werden beim Cloudflare-Deploy gesetzt.
 * Leer = Feature deaktiviert (kein kaputtes Beacon/Script im Build).
 */

/** Cloudflare Web Analytics Token (cookieless, ohne Einwilligungsbanner). Leer = aus. */
export const CF_ANALYTICS_TOKEN = '';
