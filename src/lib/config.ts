/**
 * Build-Zeit-Konfiguration. Leer = Feature deaktiviert (kein kaputtes Beacon/Script im Build).
 */

/**
 * Cloudflare Web Analytics Token (cookieless). Leer = aus.
 * Hinweis: funktioniert NUR, wenn die Seite hinter Cloudflare ausgeliefert wird.
 * Beim Hosting auf Railway hier leer lassen und ggf. einen anderen, datensparsamen
 * (cookielosen) Analytics-Dienst integrieren (Datenschutzerklaerung dann anpassen).
 */
export const CF_ANALYTICS_TOKEN = '';
